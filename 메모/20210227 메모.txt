20210227 메모



window 객체와 Document 객체

window.(함수) 객체: HTML 안 내용을 수정하거나 조작 할 때 사용 (브라우저의 새 창,탭을 열 때)
Document.(함수) 객체: 우리가 보고 쓰는 브라우저를 조작 할 때 사용 (브라우저에 나타나는 것들)


//예제// 
개발자도구 console콘솔 에서 해 본 것

var win = window.open('http://www.naver.com')
(변수이름)= 연다 ('주소이름')    

이렇게 네이버 브라우저를 열 수 있다.  win=네이버가 열려있다 를 정의한 후에 

win = closed 
윈 함수는=닫혀있다     를 쳐보면 네이버는 열려있기 때문에

false
거짓(네이버는 닫혀있지 않아 거짓)      이 나온다. 

이제 네이버 브라우저를 닫은 후 다시 

win = closed       를 치면 네이버가 닫혔기 때문에

true
참(네이버가 닫혀있어 참) 이 나온다.


localstorage로컬스토리지
로컬스토리지에 저장된 데이터는 직접 지우기 전까지 사라지지 않는다.

Array
문자열을 배열로, 배열을 문자열로 변환하는법

문자열: 각 문자(문장)이 나열되어있는 것 ['고구마','감자','옥수수']
배열: 여러 값이 한 배열 안에 들어있는 것 ["고구마,감자,옥수수']

//예제//
a에 문자열을 넣고, 배열로 바꾸고, 다시 문자열로 바꾸기

var a=['고구마','감자','옥수수'];
변수 a에 '고구마','감자','옥수수' 라는 문자열을 넣는다.
(a를 실행시키면 방금 넣은 ['고구마','감자','옥수수']; 라는 문자열이 나타난다.)
//문자열을 변수에 넣음

a.join(); 
join() 이라는 함수에 a를 넣는다.
그러면 똑같이 a를 실행시켰을 때 '고구마,감자,옥수수' 라는 배열이 나타난다.
ㄴjoin() 함수를 이용해 문자열을 배열로 바꿀 수 있다.
//문자열을 배열로 바꿈

JSON.stringify(a);
이번에는 제이슨.스트링기파이 라는 함수에 a를 넣고 실행시킨다. (a에는 문자열에서 바뀐 배열이 들어있다.)
그러면 ['고구마','감자','옥수수']; 로 배열이 다시 문자열로 바뀐다.
//문자열에서 바뀐 배열을 다시 문자열로 바꿈
-요것들을 직렬화 라고 한다. 직렬화를 다시 바꾼 것을 역직렬화라고 한다.

- JSON
parse와 stringify 두가지로 나타난다.
자바스크립트의 데이터를 네트워크로 보내거나, 내부에 저장 할 때 씀


여태까지 데이터를 보관하던 함수를 지우고, 로컬스토리지를 만들어 로컬히스토리에 데이터를 저장, 
아래에서 쓰던 워드히스토리가 없어져 오류가 나니 이 값들을 로컬스토리지에 넣어줘야 함.







